#include <stdio.h>
#include <string.h>
#include "header.h"

int main() {
    Tree proffesionTree;
    int totalProffesion;
    scanf("%d", &totalProffesion);

    Container tempContainer;
    createList(tempContainer.skill);
    for(int i = 0; i < totalProffesion; i++) {
        scanf("%s")
    }
    return 0;
}